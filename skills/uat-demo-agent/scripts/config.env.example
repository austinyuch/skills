# Example global configuration for the uat-demo-agent deliverable skill.

# Optional: point to an explicitly installed CLI binary.
# UATDEMO_BIN=/usr/local/bin/uatdemo

# Optional: default global state directory.
# Project-local config or env override may replace this. Avoid skill-install folders.
# UATDEMO_STATE_DIR=${HOME}/.local/state/uatdemo
