[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "myproject"
version = "0.1.0"
requires-python = ">=3.8"
dependencies = [
    "numpy>=1.20",
]

[tool.maturin]
python-source = "python"
module-name = "myproject.rust_ext"
